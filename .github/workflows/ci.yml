name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  rust-tests:
    name: Rust Tests
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install OpenCV and LLVM dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y pkg-config clang libclang-dev llvm-dev wget cmake build-essential
        # Install LLVM 18 which has the proper libclang.so symlink
        sudo apt-get install -y llvm-18 llvm-18-dev llvm-18-tools libclang1-18 libclang-cpp18
        # Set environment variables for LLVM 18
        echo "LIBCLANG_PATH=/usr/lib/llvm-18/lib" >> $GITHUB_ENV
        echo "LLVM_CONFIG_PATH=/usr/bin/llvm-config-18" >> $GITHUB_ENV
        echo "LD_LIBRARY_PATH=/usr/lib/llvm-18/lib:$LD_LIBRARY_PATH" >> $GITHUB_ENV
        # Verify the key files exist
        ls -la /usr/lib/llvm-18/lib/libclang.so
        which llvm-config-18

        # Build OpenCV 4.12 from source since Ubuntu 24.04 only has 4.6
        echo "Building OpenCV 4.12 from source..."
        cd /tmp
        wget -q https://github.com/opencv/opencv/archive/4.12.0.tar.gz
        tar -xzf 4.12.0.tar.gz
        cd opencv-4.12.0
        mkdir build
        cd build
        cmake \
          -DCMAKE_BUILD_TYPE=RELEASE \
          -DCMAKE_INSTALL_PREFIX=/usr/local \
          -DWITH_CUDA=OFF \
          -DWITH_OPENGL=OFF \
          -DWITH_OPENCL=OFF \
          -DWITH_TBB=OFF \
          -DWITH_IPP=OFF \
          -DWITH_1394=OFF \
          -DWITH_V4L=OFF \
          -DWITH_GTK=OFF \
          -DBUILD_TESTS=OFF \
          -DBUILD_PERF_TESTS=OFF \
          -DBUILD_EXAMPLES=OFF \
          -DBUILD_opencv_python2=OFF \
          -DBUILD_opencv_python3=OFF \
          -DBUILD_opencv_java=OFF \
          -DOPENCV_GENERATE_PKGCONFIG=ON \
          ..
        make -j$(nproc)
        sudo make install
        sudo ldconfig

        # Set OpenCV environment variables
        echo "PKG_CONFIG_PATH=/usr/local/lib/pkgconfig:$PKG_CONFIG_PATH" >> $GITHUB_ENV
        echo "LD_LIBRARY_PATH=/usr/local/lib:$LD_LIBRARY_PATH" >> $GITHUB_ENV

    - name: Install Rust toolchain
      uses: dtolnay/rust-toolchain@master
      with:
        toolchain: 1.89
        components: rustfmt, clippy

    - name: Cache cargo registry
      uses: actions/cache@v4
      with:
        path: ~/.cargo/registry
        key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}

    - name: Cache cargo index
      uses: actions/cache@v4
      with:
        path: ~/.cargo/git
        key: ${{ runner.os }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}

    - name: Cache cargo target
      uses: actions/cache@v4
      with:
        path: target
        key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('**/Cargo.lock') }}

    - name: Check formatting
      run: cargo fmt --all -- --check

    - name: Run clippy
      run: cargo clippy --all-targets --no-default-features --features python-bindings,simd,opencv -- -D warnings

    - name: Run tests (no Python bindings)
      run: cargo test --no-default-features --features simd,opencv

    - name: Compile tests (Linux compatible features)
      run: cargo test --no-default-features --features python-bindings,simd,opencv --no-run

  python-tests:
    name: Python Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
    - uses: actions/checkout@v4

    - name: Install OpenCV and LLVM dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y pkg-config clang libclang-dev llvm-dev wget cmake build-essential
        # Install LLVM 18 which has the proper libclang.so symlink
        sudo apt-get install -y llvm-18 llvm-18-dev llvm-18-tools libclang1-18 libclang-cpp18
        # Set environment variables for LLVM 18
        echo "LIBCLANG_PATH=/usr/lib/llvm-18/lib" >> $GITHUB_ENV
        echo "LLVM_CONFIG_PATH=/usr/bin/llvm-config-18" >> $GITHUB_ENV
        echo "LD_LIBRARY_PATH=/usr/lib/llvm-18/lib:$LD_LIBRARY_PATH" >> $GITHUB_ENV
        # Verify the key files exist
        ls -la /usr/lib/llvm-18/lib/libclang.so
        which llvm-config-18

        # Build OpenCV 4.12 from source since Ubuntu 24.04 only has 4.6
        echo "Building OpenCV 4.12 from source..."
        cd /tmp
        wget -q https://github.com/opencv/opencv/archive/4.12.0.tar.gz
        tar -xzf 4.12.0.tar.gz
        cd opencv-4.12.0
        mkdir build
        cd build
        cmake \
          -DCMAKE_BUILD_TYPE=RELEASE \
          -DCMAKE_INSTALL_PREFIX=/usr/local \
          -DWITH_CUDA=OFF \
          -DWITH_OPENGL=OFF \
          -DWITH_OPENCL=OFF \
          -DWITH_TBB=OFF \
          -DWITH_IPP=OFF \
          -DWITH_1394=OFF \
          -DWITH_V4L=OFF \
          -DWITH_GTK=OFF \
          -DBUILD_TESTS=OFF \
          -DBUILD_PERF_TESTS=OFF \
          -DBUILD_EXAMPLES=OFF \
          -DBUILD_opencv_python2=OFF \
          -DBUILD_opencv_python3=OFF \
          -DBUILD_opencv_java=OFF \
          -DOPENCV_GENERATE_PKGCONFIG=ON \
          ..
        make -j$(nproc)
        sudo make install
        sudo ldconfig

        # Set OpenCV environment variables
        echo "PKG_CONFIG_PATH=/usr/local/lib/pkgconfig:$PKG_CONFIG_PATH" >> $GITHUB_ENV
        echo "LD_LIBRARY_PATH=/usr/local/lib:$LD_LIBRARY_PATH" >> $GITHUB_ENV

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install Rust toolchain
      uses: dtolnay/rust-toolchain@master
      with:
        toolchain: 1.89
        components: rustfmt, clippy

    - name: Install maturin (Linux/macOS)
      if: runner.os != 'Windows'
      run: pip install maturin[patchelf]

    - name: Build Python package (Linux)
      if: runner.os == 'Linux'
      run: |
        mkdir -p dist
        maturin build --release --out dist --no-default-features --features python-bindings,simd,opencv

    - name: Build Python package (macOS)
      if: runner.os == 'macOS'
      run: |
        mkdir -p dist
        maturin build --release --out dist --no-default-features --features python-bindings,simd,metal

    - name: Install built package (Linux/macOS)
      run: pip install dist/*.whl

    - name: Install test dependencies
      run: pip install pytest pytest-benchmark numpy pillow opencv-python

    - name: Run Python tests
      run: pytest tests/ -v

  pre-commit:
    name: Pre-commit hooks
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install OpenCV and LLVM dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y pkg-config clang libclang-dev llvm-dev wget cmake build-essential
        # Install LLVM 18 which has the proper libclang.so symlink
        sudo apt-get install -y llvm-18 llvm-18-dev llvm-18-tools libclang1-18 libclang-cpp18
        # Set environment variables for LLVM 18
        echo "LIBCLANG_PATH=/usr/lib/llvm-18/lib" >> $GITHUB_ENV
        echo "LLVM_CONFIG_PATH=/usr/bin/llvm-config-18" >> $GITHUB_ENV
        echo "LD_LIBRARY_PATH=/usr/lib/llvm-18/lib:$LD_LIBRARY_PATH" >> $GITHUB_ENV
        # Verify the key files exist
        ls -la /usr/lib/llvm-18/lib/libclang.so
        which llvm-config-18

        # Build OpenCV 4.12 from source since Ubuntu 24.04 only has 4.6
        echo "Building OpenCV 4.12 from source..."
        cd /tmp
        wget -q https://github.com/opencv/opencv/archive/4.12.0.tar.gz
        tar -xzf 4.12.0.tar.gz
        cd opencv-4.12.0
        mkdir build
        cd build
        cmake \
          -DCMAKE_BUILD_TYPE=RELEASE \
          -DCMAKE_INSTALL_PREFIX=/usr/local \
          -DWITH_CUDA=OFF \
          -DWITH_OPENGL=OFF \
          -DWITH_OPENCL=OFF \
          -DWITH_TBB=OFF \
          -DWITH_IPP=OFF \
          -DWITH_1394=OFF \
          -DWITH_V4L=OFF \
          -DWITH_GTK=OFF \
          -DBUILD_TESTS=OFF \
          -DBUILD_PERF_TESTS=OFF \
          -DBUILD_EXAMPLES=OFF \
          -DBUILD_opencv_python2=OFF \
          -DBUILD_opencv_python3=OFF \
          -DBUILD_opencv_java=OFF \
          -DOPENCV_GENERATE_PKGCONFIG=ON \
          ..
        make -j$(nproc)
        sudo make install
        sudo ldconfig

        # Set OpenCV environment variables
        echo "PKG_CONFIG_PATH=/usr/local/lib/pkgconfig:$PKG_CONFIG_PATH" >> $GITHUB_ENV
        echo "LD_LIBRARY_PATH=/usr/local/lib:$LD_LIBRARY_PATH" >> $GITHUB_ENV

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.11

    - name: Install Rust toolchain
      uses: dtolnay/rust-toolchain@master
      with:
        toolchain: 1.89
        components: rustfmt, clippy

    - name: Install cargo-audit
      run: cargo install cargo-audit

    - name: Install pre-commit
      run: pip install pre-commit

    - name: Run pre-commit
      run: pre-commit run --all-files

  build-wheels:
    name: Build wheels
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
    - uses: actions/checkout@v4

    - name: Install OpenCV and LLVM dependencies (Ubuntu)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y pkg-config clang libclang-dev llvm-dev wget cmake build-essential
        # Install LLVM 18 which has the proper libclang.so symlink
        sudo apt-get install -y llvm-18 llvm-18-dev llvm-18-tools libclang1-18 libclang-cpp18
        # Set environment variables for LLVM 18
        echo "LIBCLANG_PATH=/usr/lib/llvm-18/lib" >> $GITHUB_ENV
        echo "LLVM_CONFIG_PATH=/usr/bin/llvm-config-18" >> $GITHUB_ENV
        echo "LD_LIBRARY_PATH=/usr/lib/llvm-18/lib:$LD_LIBRARY_PATH" >> $GITHUB_ENV
        # Verify the key files exist
        ls -la /usr/lib/llvm-18/lib/libclang.so
        which llvm-config-18

        # Build OpenCV 4.12 from source since Ubuntu 24.04 only has 4.6
        echo "Building OpenCV 4.12 from source..."
        cd /tmp
        wget -q https://github.com/opencv/opencv/archive/4.12.0.tar.gz
        tar -xzf 4.12.0.tar.gz
        cd opencv-4.12.0
        mkdir build
        cd build
        cmake \
          -DCMAKE_BUILD_TYPE=RELEASE \
          -DCMAKE_INSTALL_PREFIX=/usr/local \
          -DWITH_CUDA=OFF \
          -DWITH_OPENGL=OFF \
          -DWITH_OPENCL=OFF \
          -DWITH_TBB=OFF \
          -DWITH_IPP=OFF \
          -DWITH_1394=OFF \
          -DWITH_V4L=OFF \
          -DWITH_GTK=OFF \
          -DBUILD_TESTS=OFF \
          -DBUILD_PERF_TESTS=OFF \
          -DBUILD_EXAMPLES=OFF \
          -DBUILD_opencv_python2=OFF \
          -DBUILD_opencv_python3=OFF \
          -DBUILD_opencv_java=OFF \
          -DOPENCV_GENERATE_PKGCONFIG=ON \
          ..
        make -j$(nproc)
        sudo make install
        sudo ldconfig

        # Set OpenCV environment variables
        echo "PKG_CONFIG_PATH=/usr/local/lib/pkgconfig:$PKG_CONFIG_PATH" >> $GITHUB_ENV
        echo "LD_LIBRARY_PATH=/usr/local/lib:$LD_LIBRARY_PATH" >> $GITHUB_ENV

    - name: Install OpenCV and LLVM dependencies (Windows)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        $ErrorActionPreference = "Stop"
        choco install -y llvm --version=18.1.8

        # Point bindgen at LLVM 18
        $llvmBin = "C:\\Program Files\\LLVM\\bin"
        echo "LIBCLANG_PATH=$llvmBin" >> $env:GITHUB_ENV
        echo "LLVM_CONFIG_PATH=$llvmBin\\llvm-config.exe" >> $env:GITHUB_ENV

        # Build OpenCV 4.12 statically with a minimal feature set
        $work = Join-Path $env:RUNNER_TEMP "opencv"
        $source = Join-Path $env:RUNNER_TEMP "opencv-4.12.0"
        $build = Join-Path $source "build"
        $zip = Join-Path $env:RUNNER_TEMP "opencv-4.12.0.zip"
        New-Item -ItemType Directory -Path $work -Force | Out-Null
        Invoke-WebRequest -Uri https://github.com/opencv/opencv/archive/4.12.0.zip -OutFile $zip
        Expand-Archive -Path $zip -DestinationPath $env:RUNNER_TEMP
        cmake -S $source -B $build -G "Visual Studio 17 2022" -A x64 `
          -DCMAKE_INSTALL_PREFIX="$work" `
          -DBUILD_SHARED_LIBS=OFF `
          -DBUILD_opencv_world=ON `
          -DWITH_CUDA=OFF `
          -DWITH_OPENGL=OFF `
          -DWITH_OPENCL=OFF `
          -DWITH_TBB=OFF `
          -DWITH_IPP=OFF `
          -DWITH_1394=OFF `
          -DWITH_GSTREAMER=OFF `
          -DBUILD_TESTS=OFF `
          -DBUILD_PERF_TESTS=OFF `
          -DBUILD_EXAMPLES=OFF `
          -DBUILD_opencv_python2=OFF `
          -DBUILD_opencv_python3=OFF `
          -DBUILD_JPEG=ON -DWITH_JPEG=ON `
          -DBUILD_PNG=ON -DWITH_PNG=ON `
          -DBUILD_TIFF=ON -DWITH_TIFF=ON `
          -DBUILD_WEBP=ON -DWITH_WEBP=ON `
          -DBUILD_ZLIB=ON -DWITH_ZLIB=ON `
          -DOPENCV_GENERATE_PKGCONFIG=ON
        cmake --build $build --config Release --target install

        # Export OpenCV paths for the Rust build script based on what was installed
        $includeCandidate = Get-ChildItem -Path $work -Recurse -Directory -Filter "opencv2" -ErrorAction SilentlyContinue | Select-Object -First 1
        if (-not $includeCandidate) { throw "OpenCV include path not found" }
        $includeRoot = Split-Path $includeCandidate.FullName
        $includePaths = @($includeRoot)
        $opencv4 = Join-Path $includeRoot "opencv4"
        if (Test-Path $opencv4) { $includePaths += $opencv4 }

        # Find the installed OpenCV world library (static or import) and derive its name for linking
        $libCandidate = Get-ChildItem -Path $work -Recurse -File -Include "*opencv_world*.lib","*opencv_world*.a" -ErrorAction SilentlyContinue | Select-Object -First 1
        if (-not $libCandidate) { throw "OpenCV lib path not found" }
        $libPath = Split-Path $libCandidate.FullName
        $libName = [IO.Path]::GetFileNameWithoutExtension($libCandidate.Name) -replace '^lib',''

        $cmakeConfig = Get-ChildItem -Path $work -Recurse -File -Filter "OpenCVConfig.cmake" -ErrorAction SilentlyContinue | Select-Object -First 1
        if (-not $cmakeConfig) { throw "OpenCV CMake config path not found" }
        $cmakeDir = Split-Path $cmakeConfig.FullName

        echo "OPENCV_INCLUDE_PATHS=$($includePaths -join ';')" >> $env:GITHUB_ENV
        echo "OPENCV_LINK_PATHS=$libPath" >> $env:GITHUB_ENV
        echo "OPENCV_LINK_LIBS=static=$libName" >> $env:GITHUB_ENV
        echo "OPENCV_VERSION=4.12.0" >> $env:GITHUB_ENV
        echo "OPENCV_DISABLE_PROBES=pkg_config" >> $env:GITHUB_ENV
        echo "OpenCV_DIR=$cmakeDir" >> $env:GITHUB_ENV
        echo "CMAKE_PREFIX_PATH=$cmakeDir" >> $env:GITHUB_ENV


    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.11

    - name: Install Rust toolchain
      uses: dtolnay/rust-toolchain@master
      with:
        toolchain: 1.89

    - name: Install maturin (Linux/macOS)
      if: runner.os != 'Windows'
      run: pip install maturin[patchelf]

    - name: Install maturin (Windows)
      if: runner.os == 'Windows'
      run: pip install maturin

    - name: Install OpenCV (macOS)
      if: runner.os == 'macOS'
      run: |
        # Install OpenCV and LLVM via Homebrew
        brew install opencv llvm
        # Set up LLVM environment for opencv-rust bindings (keg-only installation)
        echo "LIBCLANG_PATH=/opt/homebrew/opt/llvm/lib" >> $GITHUB_ENV
        echo "LLVM_CONFIG_PATH=/opt/homebrew/opt/llvm/bin/llvm-config" >> $GITHUB_ENV
        echo "DYLD_LIBRARY_PATH=/opt/homebrew/opt/llvm/lib:$DYLD_LIBRARY_PATH" >> $GITHUB_ENV
        # Set up OpenCV environment
        echo "OPENCV_LINK_LIBS=opencv_calib3d,opencv_core,opencv_dnn,opencv_features2d,opencv_flann,opencv_highgui,opencv_imgcodecs,opencv_imgproc,opencv_ml,opencv_objdetect,opencv_photo,opencv_stitching,opencv_video,opencv_videoio" >> $GITHUB_ENV
        echo "OPENCV_LINK_PATHS=/opt/homebrew/lib" >> $GITHUB_ENV
        echo "OPENCV_INCLUDE_PATHS=/opt/homebrew/include/opencv4" >> $GITHUB_ENV

    - name: Build wheels (Linux)
      if: runner.os == 'Linux'
      run: maturin build --release --out dist --no-default-features --features python-bindings,simd,opencv

    - name: Build wheels (macOS)
      if: runner.os == 'macOS'
      run: maturin build --release --out dist --no-default-features --features python-bindings,simd,opencv,metal

    - name: Build wheels (Windows)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        $ErrorActionPreference = "Stop"
        mkdir dist
        maturin build --release --out dist --no-default-features --features python-bindings,simd,opencv


    - name: Upload wheels
      uses: actions/upload-artifact@v4
      with:
        name: wheels-${{ matrix.os }}
        path: dist/
